import { Component } from '@angular/core';
@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

  // constructor(private schoolService: SchoolService, private school: School) {

  // }  
  
  // this.schoolService.getSchool(School).subscribe(data => {
  // if (data.success) {
  //   console.log(data.success);
  //   console.log(data);
  //   }
  // }
}